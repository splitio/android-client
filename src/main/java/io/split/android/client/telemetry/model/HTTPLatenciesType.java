package io.split.android.client.telemetry.model;

public enum HTTPLatenciesType {
    SPLITS,
    SEGMENTS,
    IMPRESSIONS,
    IMPRESSIONS_COUNT,
    EVENTS,
    TELEMETRY,
    TOKEN,
    MY_SEGMENT
}
